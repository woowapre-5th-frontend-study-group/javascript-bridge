const BridgeSizeValidator = require('./BridgeSizeValidator');
const MovingValidator = require('./MovingValidator');
const GameCommandValidator = require('./GameCommandValidator');

const Validator = {
  BridgeSizeValidator,
  MovingValidator,
  GameCommandValidator,
};

module.exports = Validator;
